# Print Server Configuration
# Copy this to .env and update with your settings

# Printer name as shown in Windows (Get-Printer)
PRINTER_NAME=EPSON TM-T82II Receipt

# Server port
PORT=3000

# Network interface (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
HOST=0.0.0.0
